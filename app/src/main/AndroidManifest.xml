<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yuanzi.ting.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.SimpleActivity" />
        <activity android:name=".Activity.MoreActivity" />
        <activity android:name=".Activity.NetworkActivity" />
        <activity android:name=".Activity.BindingActivity" />
        <activity android:name=".Activity.LambdaActivity" />
        <activity android:name=".Activity.SafeActivity" />
        <activity android:name=".Activity.NetworkDetailActivity" />
        <activity android:name=".Activity.TestDataBindingActivity"></activity>
    </application>

</manifest>